global:
  resolve_timeout: 5m

route:
  receiver: pagerduty
  group_by: ['alertname', 'pod', 'namespace']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 60s

receivers:
  - name: pagerduty
    pagerduty_configs:
      - routing_key: "your-pager-duty-intergration-key"
        severity: '{{ .CommonLabels.severity }}'
        description: '{{ .CommonAnnotations.summary }}'
